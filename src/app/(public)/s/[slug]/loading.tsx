import { LoadingPanel } from "@/components/route-state-panel";

export default function LoadingSubspace() {
  return (
    <LoadingPanel
      description="Fetching the subspace details and recent post information."
      title="Loading subspace"
    />
  );
}
